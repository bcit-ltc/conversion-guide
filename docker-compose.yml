name: ${APP_NAME}

services:
  app:
    image: ${APP_NAME}
    build:
      target: cleaner
    environment:
      - DEBUG=True
    working_dir: /app
    volumes:
      - .:/app
      - ./conf.d/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8080:8080"
