name: ${APP_NAME:-conversion-guide}
services:
  app:
    build:
      context: .
      target: release
    environment:
      - DEBUG=True
    working_dir: /app
    volumes:
      - .:/app
      - ./conf.d/default.conf:/etc/nginx/conf.d/default.conf
    ports:
      - "8080:8080"
